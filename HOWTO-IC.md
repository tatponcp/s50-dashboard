# 📝 คู่มือ IC — วิธีเข้าไปกรอก Morning Brief ทุกเช้า

คู่มือนี้เขียนไว้สำหรับ**คนกรอกข้อมูล (IC)** โดยเฉพาะ ไม่ต้องรู้เรื่องโค้ด
ทำตามทีละขั้นได้เลย ถ้าติดตรงไหนกลับมาดูหน้านี้ได้ตลอด

> อยากรู้เรื่องระบบเบื้องหลังทั้งหมด (scraper, Supabase, deploy) ดูที่
> `HANDBOOK.md` แทน — ไฟล์นี้พูดแค่ "จะเข้าไปกรอกยังไง" อย่างเดียว

---

## ขั้นที่ 0 — ของที่ต้องมีในเครื่อง (เช็คครั้งเดียว)

ต้องมี **Python** ในเครื่อง (ใช้เปิดเครื่องมือกรอกข้อมูล)

เปิด **PowerShell** แล้วพิมพ์:
```
py --version
```
ถ้าขึ้นเลขเวอร์ชัน (เช่น `Python 3.14.6`) แปลว่าใช้ได้เลย ข้ามไปขั้นที่ 1
ถ้าขึ้น error ให้บอกทีมโค้ดเพื่อติดตั้งให้ก่อน

---

## ขั้นที่ 1 — เปิด "เครื่องมือกรอกข้อมูล" (ทำทุกครั้งก่อนเริ่มงาน)

1. เปิด **PowerShell**
2. พิมพ์คำสั่งนี้เพื่อไปที่โฟลเดอร์โปรเจกต์ (copy ทั้งบรรทัดไปวางได้เลย):
   ```powershell
   cd "C:\Users\tatpo\OneDrive\internship\Cool Project\Oi Dashboard Fable5"
   ```
3. พิมพ์คำสั่งนี้เพื่อ "เปิดเครื่องเสิร์ฟ" (server) — **ห้ามปิดหน้าต่างนี้ระหว่างทำงาน**:
   ```powershell
   py -m http.server 8123
   ```
   ถ้าสำเร็จจะเห็นข้อความประมาณ `Serving HTTP on :: port 8123` ค้างอยู่ที่หน้าจอ
   (ปกติ — แปลว่ากำลังทำงานอยู่ ไม่ใช่ค้าง)
4. เปิดเบราว์เซอร์ (Chrome/Edge) แล้วพิมพ์ URL นี้ในช่อง address bar:
   ```
   http://localhost:8123/internal/edit.html
   ```
5. จะเห็นหน้า **"IC Console — Morning Brief"** ขึ้นมา → พร้อมกรอกข้อมูลแล้ว

**ทำไมต้องทำแบบนี้ (เปิดเป็นขั้นตอน ไม่ใช่ดับเบิลคลิกไฟล์ตรงๆ)?**
เพราะถ้าดับเบิลคลิกไฟล์ `edit.html` ตรงๆ เบราว์เซอร์จะบล็อกไม่ให้โหลดรูป/ข้อมูล
หน้าจะว่างเปล่า ต้องเปิดผ่าน `http://localhost...` เท่านั้นถึงจะใช้งานได้ครบ

**ปิดงานยังไง?** กลับไปที่หน้าต่าง PowerShell ที่รัน server ค้างไว้ กด `Ctrl+C`

---

## ขั้นที่ 2 — กรอกข้อมูลแต่ละหัวข้อ (มี 6 หัวข้อ)

เลื่อนหน้าจอลงมาจะเจอฟอร์มทีละหัวข้อ (1. S50 Futures + OI, 2. ต่างชาติ/กองทุน, ...)
ทำทีละหัวข้อตามลำดับนี้:

1. **แปะภาพกราฟ** — คลิกที่กล่อง "ภาพกราฟ" แล้ว `Ctrl+V` (ถ้า copy รูปมาจากที่อื่น)
   หรือลากไฟล์รูปมาวางในกล่องได้เลย
2. **กรอกตัวเลขสำคัญ** เช่น Close, Change (กดปุ่ม "+ เพิ่มแถว")
3. **เลือกสัญญาณวันนี้** จาก dropdown 2 ช่องด้านล่างภาพ (ดูจากภาพกราฟที่เพิ่งแปะ)
   เช่น "ราคา S50 วันนี้ = ลง", "Open Interest วันนี้ = เพิ่มขึ้น"
4. กดปุ่มสีทอง **🪄 สร้างข้อความอัตโนมัติจากสัญญาณ**
   → ระบบจะเติมข้อความ "แปลความ / Action วันนี้ / Insight" ให้เองทันที
5. **อ่านทวนข้อความที่เติมมา** — ถ้าไม่ตรงกับภาพจริงวันนี้ พิมพ์แก้ทับในช่องได้เลย
   (ไม่ต้องกดปุ่มซ้ำ พิมพ์แก้ตรงๆ ได้)

ทำซ้ำแบบนี้ให้ครบทั้ง 6 หัวข้อ

> 💾 **ไม่ต้องกดเซฟ** — ระบบเซฟให้อัตโนมัติทุกครั้งที่พิมพ์ (เก็บไว้ในเครื่องนี้เครื่องเดียว
> ถ้าปิดเบราว์เซอร์แล้วเปิดใหม่ ข้อมูลที่กรอกค้างไว้จะยังอยู่)

---

## ขั้นที่ 3 — ดูตัวอย่างก่อนส่งจริง

กดปุ่ม **Preview** ที่แถบด้านบนสุด → จะเห็นหน้าตาแบบเดียวกับที่ลูกค้าจะเห็นจริง
เลื่อนดูให้ครบทุกหัวข้อ ถ้าตรงไหนยังไม่โอเค กดปุ่ม **กรอกข้อมูล** (ข้างๆ Preview) เพื่อกลับไปแก้

---

## ขั้นที่ 4 — ส่งให้ลูกค้าเห็น (Publish)

เมื่อเช็คครบแล้ว กดปุ่มเขียว **Approve & Publish ทั้งหน้า** มุมขวาบนสุด

เบราว์เซอร์จะ **ดาวน์โหลดไฟล์ให้เองหลายไฟล์** (เข้าไปดูที่โฟลเดอร์ **Downloads**) —
ลากไฟล์เหล่านั้นไปวางตามนี้:

| ไฟล์ที่ได้ | ลากไปวางที่โฟลเดอร์ |
|---|---|
| ไฟล์รูป `.png` | `client/images/` |
| ไฟล์ `2026-XX-XX.json` และ `index.json` | `client/data/` |

จากนั้นบอกทีมโค้ด (หรือถ้าทำเองได้) เปิด PowerShell แล้วพิมพ์ 3 บรรทัดนี้:
```powershell
git add client/ shared/
git commit -m "publish morning brief วันที่ XX"
git push
```
รอ 1-2 นาที → ลูกค้าเปิดเว็บก็จะเห็นข้อมูลใหม่ทันที จบงาน 🎉

---

## สรุปสั้นที่สุด (แปะไว้เตือนตัวเอง)

```
1. เปิด PowerShell → py -m http.server 8123
2. เปิดเบราว์เซอร์ → http://localhost:8123/internal/edit.html
3. กรอกทีละหัวข้อ → เลือกสัญญาณ → กด 🪄
4. กด Preview เช็ค
5. กด Approve & Publish → ลากไฟล์จาก Downloads ไปวาง
6. git add / commit / push
```

---

## ถ้าติดปัญหา

| อาการ | วิธีแก้ |
|---|---|
| เปิด `http://localhost:8123/...` แล้วขึ้น "ไม่พบเว็บไซต์" | ลืมเปิด server (ขั้นที่ 1 ข้อ 3) — เช็คว่าหน้าต่าง PowerShell ยังเปิดค้างอยู่ไหม |
| หน้าเว็บว่างเปล่า ไม่มีฟอร์ม | เปิดผิด URL — ต้องขึ้นต้นด้วย `http://localhost:8123/` ห้ามดับเบิลคลิกไฟล์ตรงๆ |
| กรอกไว้แล้วหาย | ปกติจะไม่หาย (เซฟอัตโนมัติ) ยกเว้นกดปุ่ม "ล้าง draft" — ปุ่มนี้ล้างจริง ต้อง publish ก่อนกดเสมอ |
| ปุ่ม 🪄 กดแล้วไม่มีอะไรเกิดขึ้น / มี alert เตือน | ยังเลือก dropdown สัญญาณไม่ครบทุกช่อง — เลือกให้ครบก่อนกด |
| กด Publish แล้วหาไฟล์ใน Downloads ไม่เจอ | เช็คการตั้งค่าเบราว์เซอร์ว่าดาวน์โหลดไปที่โฟลเดอร์ไหน (บางเครื่องตั้งไว้ที่อื่น) |
